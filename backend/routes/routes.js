const express = require('express');
const router = express.Router();

router.get('/', (req, res) => {   //get all products from database

})

router.get('/:id', (req, res) => {   //get specific product from database

})

module.exports = router;